# Cloudbuilder

```
bash main.sh --list-existing
bash main.sh --list-available

```

```
bash main.sh --storage local-zfs --template debian-11 --template debian-12 --template alma-8 --template alma-9 --template ubuntu-22-04 --template ubuntu-23-04 --template ubuntu-24-04
```
